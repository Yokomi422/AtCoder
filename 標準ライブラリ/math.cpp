#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
using Graph = vector<vector<int>>;
using ll = long long;

int main() {
    //冪乗a**b pow(a,b)
    // 平方根 sqrt()
    //対数 log2()
    //accumulateは総和などを求められる
    //gcdは最大公約数,lcmは最小公倍数
    //切り上げはceil このような関数は大きくなると正確性に欠けがち. x/y. 切り捨てや切り上げなどは正確に行う必要がある。
    int ans = (x + y - 1) / y;
}